{
  "name": "@bstester/quant-platform-v3",
  "private": true,
  "version": "3.0.0-alpha.1",
  "type": "module",
  "description": "Harness-Centric 量化交易决策平台 V3.0：平台 MCP 服务器（六域工具·发现代理）+ 三通道网关（MCP/SDK/Headless）+ 调度器 + 风控 + Web UI。数据来源复用既有 workbench(8397) 的 77 工具面与既有配置，不替代不破坏。",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "start:mcp": "node server/mcp/stdio.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
