---
name: futu-capital-alerts
description: 资金面异动观察（主力资金流/南向/量价配合）——当用户问资金流向、主力进出或量价背离时使用；工作台 flow_* 工具属 WP8 任务 5 待交付，当前用只读通道降级并如实标注。
---

# 资金面异动（capital_analysis · 工作台对接版）

参考富途官方 SkillHub 资金面异动 skill 的分析框架，数据通道对接工作台；
**工作台侧的资金流工具族尚未交付，当前按降级路径取数并如实标注**。

## 数据通道

| 数据点 | 工具 | 说明 |
|---|---|---|
| 资金流（首选，**待交付**） | WP8 规划的 `flow_*` 工具族（capital-flow/-history/-distribution 映射） | **WP8 任务 5 交付后可用**；当前调用返回不可用属预期——不要反复重试，直接走降级路径 |
| 资金流（降级，只读） | `mcp__futu__capital_flow`（只读） | 分钟级资金流；A 股实测可用（见 docs/TOOL-LIMITS.md）。输出标注来源与时间 |
| 量能配合 | `mcp__quantwb__series` | K 线成交量，做量价背离判断 |
| 通道健康 | `mcp__quantwb__sources` | 确认各数据渠道可用性与 as_of，再引用数据 |

- 降级纪律：降级数据必须注明"来源：富途只读通道（非工作台）+ 数据时间"；取不到就明说，
  不要编造主力净流入等数值。
- 南向资金、大宗交易等 SkillHub 示例里的数据点：工作台与富途只读通道均无对应工具时，
  如实告知"暂无可用通道"，不得用无关数据凑数。

## 工作流程

1. `mcp__quantwb__sources` 确认通道健康；发现工作台资金流通道报不可用属预期（待交付），
   转降级路径。
2. 优先尝试规划中的 `flow_*` 工具（WP8 任务 5 交付后此步即主路径）；当前返回不可用时
   改用 `mcp__futu__capital_flow`（只读）并标注。
3. `mcp__quantwb__series` 取同期 K 线，对照资金流向做量价关系判断
   （放量上涨/缩量下跌、量价背离等）。
4. **输出格式**：

   > **资金流表**：时段 | 净流入/流出 | 主力占比 | 量能 | 数据来源 | 数据时间
   > **摘要**：一段话概括资金面方向与强度
   > **关键信号**：异动条目（如"连续 N 个分钟段净流入""量价背离"）
   > 以上内容基于公开信息整理，不构成投资建议

## 安全

- 本技能**只读**，不下单、不改单、不撤单。
- 资金面数据滞后且口径各异（主力/大单定义随源不同），结论必须带数据时间与来源；
  降级通道的数据不与工作台口径混用。
- 用户据信号要求交易时：写操作唯一路径是工作台通道（见 `futu-trading` 技能：
  quantwb `trade_*` 工具 + 用户在独立 Web 确认卡片批准）；禁止经 `mcp__futu__` 或任何
  绕过工作台的方式下单。live 写通道未接入（当前仅 sim 可交易），如实说明。
